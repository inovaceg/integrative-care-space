import { createFileRoute } from "@tanstack/react-router";
import { CalendarDays, ChevronLeft, ChevronRight, Clock3, Filter } from "lucide-react";
import { useState } from "react";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from "@/components/ui/select";
import { AdminLayout, AdminNotice, NewAppointmentDialog, PageIntro, PatientAvatar, StatusBadge } from "@/components/admin/admin-ui";
import { appointments } from "@/lib/admin/mocks";

export const Route = createFileRoute("/admin/agenda")({ component: AgendaPage });

function AgendaPage() {
  const [view, setView] = useState<"Dia" | "Semana" | "Mês">("Dia");
  return <AdminLayout><PageIntro eyebrow="Organização" title="Agenda" description="Visualize seus horários e mantenha cada atendimento sob controle." action={<NewAppointmentDialog />} /><AdminNotice /><div className="mt-6 flex flex-col justify-between gap-3 sm:flex-row sm:items-center"><div className="flex w-fit rounded-lg border border-slate-200 bg-white p-1 shadow-sm">{(["Dia", "Semana", "Mês"] as const).map((item) => <button type="button" key={item} onClick={() => setView(item)} className={`rounded-md px-4 py-2 text-sm font-medium transition-colors ${view === item ? "bg-[#e3f3ef] text-[#216d64]" : "text-slate-500 hover:text-slate-800"}`} aria-pressed={view === item}>{item}</button>)}</div><div className="flex items-center gap-2"><Button variant="outline" size="icon" className="border-slate-200 bg-white" aria-label="Período anterior"><ChevronLeft className="size-4" /></Button><Button variant="outline" className="gap-2 border-slate-200 bg-white"><CalendarDays className="size-4 text-[#2f8f82]" /> 12 jun 2025</Button><Button variant="outline" size="icon" className="border-slate-200 bg-white" aria-label="Próximo período"><ChevronRight className="size-4" /></Button></div></div><div className="mt-5 grid gap-6 xl:grid-cols-[1fr_280px]"><Card className="border-slate-200/80 shadow-sm"><CardContent className="p-0"><div className="flex items-center justify-between border-b border-slate-100 px-5 py-4"><div><h3 className="font-display font-semibold text-slate-800">Quinta-feira, 12 de junho</h3><p className="mt-1 text-xs text-slate-400">{appointments.filter((item) => item.date === "Hoje").length} atendimentos agendados</p></div><div className="flex items-center gap-2"><Select defaultValue="all"><SelectTrigger className="h-9 w-[132px] border-slate-200 bg-white text-xs"><Filter className="mr-1 size-3.5" /><SelectValue /></SelectTrigger><SelectContent><SelectItem value="all">Todos os tipos</SelectItem><SelectItem value="psychology">Psicologia</SelectItem><SelectItem value="biomedicine">Biomedicina</SelectItem></SelectContent></Select></div></div><div className="divide-y divide-slate-100">{appointments.filter((item) => item.date === "Hoje").map((appointment) => <div key={appointment.id} className="grid grid-cols-[62px_5px_1fr_auto] items-center gap-3 px-5 py-5 sm:grid-cols-[76px_5px_1fr_auto] sm:gap-4"><div className="text-right"><p className="font-display text-sm font-semibold text-slate-800">{appointment.time}</p><p className="mt-1 text-[11px] text-slate-400">{appointment.duration}</p></div><div className={`h-14 rounded-full ${appointment.color === "blue" ? "bg-[#7ba4d1]" : appointment.color === "amber" ? "bg-[#dfb968]" : "bg-[#69b9a9]"}`} /><div className="flex min-w-0 items-center gap-3"><PatientAvatar initials={appointment.initials} className="hidden sm:flex" /><div className="min-w-0"><p className="truncate text-sm font-semibold text-slate-800">{appointment.patientName}</p><p className="truncate text-xs text-slate-500">{appointment.service}</p></div></div><div className="text-right"><StatusBadge status={appointment.status} /><button type="button" className="mt-2 block text-[11px] text-[#2f8f82] hover:underline">Ver detalhes</button></div></div>)}</div></CardContent></Card><Card className="h-fit border-slate-200/80 shadow-sm"><CardContent className="p-5"><div className="flex items-center gap-2 text-[#2f8f82]"><Clock3 className="size-4" /><h3 className="font-display text-sm font-semibold text-slate-800">Resumo do dia</h3></div><div className="mt-5 space-y-4"><div><p className="text-xs text-slate-500">Primeiro atendimento</p><p className="mt-1 font-display text-lg font-semibold text-slate-800">09:00</p></div><div><p className="text-xs text-slate-500">Horas em atendimento</p><p className="mt-1 font-display text-lg font-semibold text-slate-800">4h 10min</p></div><div className="border-t border-slate-100 pt-4"><p className="text-xs leading-5 text-slate-400">A visão de {view.toLowerCase()} está ativa. Alterações são apenas visuais nesta demonstração.</p></div></div></CardContent></Card></div></AdminLayout>;
}
