import { createFileRoute, Link } from "@tanstack/react-router";
import { ArrowUpRight, CalendarPlus, MoreHorizontal } from "lucide-react";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { AdminLayout, AdminNotice, DashboardCard, PageIntro, PatientAvatar, SectionCard, StatusBadge } from "@/components/admin/admin-ui";
import { appointments, dashboardStats, patients } from "@/lib/admin/mocks";

export const Route = createFileRoute("/admin/dashboard")({ component: DashboardPage });

function DashboardPage() {
  return <AdminLayout><PageIntro eyebrow="Resumo do dia" title="Olá, Frederick 👋" description="Aqui está o que está acontecendo no seu espaço hoje." action={<Button asChild variant="outline" className="gap-2 border-slate-200 bg-white"><Link to="/admin/agenda"><CalendarPlus className="size-4" /> Ver agenda</Link></Button>} /><AdminNotice /><div className="mt-6 grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{dashboardStats.map((stat) => <DashboardCard key={stat.label} {...stat} />)}</div><div className="mt-6 grid gap-6 xl:grid-cols-[1.35fr_.65fr]"><SectionCard title="Próximos atendimentos" action={<Button asChild variant="ghost" size="sm" className="text-[#2f8f82]"><Link to="/admin/agenda">Ver agenda <ArrowUpRight className="ml-1 size-3.5" /></Link></Button>}><div className="space-y-1">{appointments.slice(0, 4).map((appointment) => <div key={appointment.id} className="flex items-center gap-3 rounded-lg px-2 py-3 transition-colors hover:bg-slate-50"><div className="w-12 shrink-0 text-center"><p className="font-display text-sm font-semibold text-slate-800">{appointment.time}</p><p className="text-[10px] text-slate-400">{appointment.duration}</p></div><div className="h-10 w-1 rounded-full bg-[#7bc7b7]" /><PatientAvatar initials={appointment.initials} /><div className="min-w-0 flex-1"><p className="truncate text-sm font-medium text-slate-800">{appointment.patientName}</p><p className="truncate text-xs text-slate-500">{appointment.service}</p></div><StatusBadge status={appointment.status} /><button type="button" aria-label={`Mais opções para ${appointment.patientName}`} className="hidden rounded-md p-1.5 text-slate-400 hover:bg-slate-100 sm:block"><MoreHorizontal className="size-4" /></button></div>)}</div></SectionCard><SectionCard title="Pacientes recentes" action={<Button asChild variant="ghost" size="sm" className="text-[#2f8f82]"><Link to="/admin/pacientes">Ver todos <ArrowUpRight className="ml-1 size-3.5" /></Link></Button>}><div className="space-y-4">{patients.slice(0, 4).map((patient) => <Link key={patient.id} to="/admin/pacientes/$id" params={{ id: patient.id }} className="flex items-center gap-3 rounded-lg p-1 transition-colors hover:bg-slate-50"><PatientAvatar initials={patient.initials} /><div className="min-w-0 flex-1"><p className="truncate text-sm font-medium text-slate-800">{patient.name}</p><p className="text-xs text-slate-500">{patient.specialty}</p></div><p className="hidden text-right text-[11px] text-slate-400 sm:block">{patient.lastAppointment}</p></Link>)}</div></SectionCard></div><Card className="mt-6 border-0 bg-[#123c3d] text-white shadow-sm"><CardContent className="flex flex-col items-start justify-between gap-4 p-5 sm:flex-row sm:items-center sm:p-6"><div><p className="text-xs font-semibold uppercase tracking-[0.16em] text-[#8bd5c4]">Acesso rápido</p><p className="mt-2 font-display text-lg font-medium">Mantenha sua rotina organizada</p><p className="mt-1 text-sm text-white/65">Consulte a agenda, registre pacientes e acompanhe seus indicadores.</p></div><Button asChild className="shrink-0 bg-[#8bd5c4] text-[#123c3d] hover:bg-[#a5e2d4]"><Link to="/admin/pacientes">Gerenciar pacientes</Link></Button></CardContent></Card></AdminLayout>;
}
