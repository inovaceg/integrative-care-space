import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.sitefredatualizadopsibio',
  appName: 'Site Fred - Atualizado Psi + Bio',
  webDir: 'dist'
};

export default config;
